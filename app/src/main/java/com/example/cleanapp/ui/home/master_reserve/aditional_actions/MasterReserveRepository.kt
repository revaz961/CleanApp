package com.example.cleanapp.ui.home.master_reserve.aditional_actions

import com.example.cleanapp.models.Master
import com.google.firebase.database.DatabaseReference
import javax.inject.Inject

class MasterReserveRepository @Inject constructor(private val dbRef:DatabaseReference){

    fun startChat(master: Master){
//        dbRef.child()
    }
}